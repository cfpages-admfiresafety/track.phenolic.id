# track.phenolic.id

Private-purpose Short.io statistics dashboard. Search indexing is disabled through `robots.txt`, a site-wide `X-Robots-Tag`, and an HTML robots meta tag. Keep crawlers allowed in `robots.txt` so they can read and honor `noindex`.
